# Custom category for connectors
Custom.category=Custom connectors

# Connector general information
connectorDefinitionDescription=Bonita connector: ${project.artifactId}
connectorDefinitionLabel=${project.artifactId}

# Page Titles and Descriptions
rabbitMQPage.pageTitle=RabbitMQ Configuration
rabbitMQPage.pageDescription=Specify the RabbitMQ connection details including host, queue name, and optional message.

queueDeclarePage.pageTitle=Queue Declaration
queueDeclarePage.pageDescription=Define the properties of the RabbitMQ queue, including durability, exclusivity, and auto-delete settings.

searchPage.pageTitle=Queue Search
searchPage.pageDescription=Search for an existing RabbitMQ queue and retrieve its details.

# Host Configuration
hostWidget.label=Host
hostWidget.description=IP address or hostname of the RabbitMQ server.

# Queue Name
queueNameWidget.label=Queue Name
queueNameWidget.description=Name of the RabbitMQ queue to be used.

# Authentication
usernameWidget.label=Username
usernameWidget.description=Username for RabbitMQ authentication.

passwordWidget.label=Password
passwordWidget.description=Password for RabbitMQ authentication.

# Queue Properties
# durableWidget.label=Durable Queue
# durableWidget.description=Indicates whether the queue should survive a broker restart.

# exclusiveWidget.label=Exclusive Queue
# exclusiveWidget.description=If enabled, the queue will only be used by the current connection and deleted when the connection closes.

# autoDeleteWidget.label=Auto-Delete Queue
# autoDeleteWidget.description=If enabled, the queue will be deleted automatically when it is no longer in use.

# argumentsWidget.label=Queue Arguments
# argumentsWidget.description=Additional queue configuration options as a Map<String, Object>. Refer to the RabbitMQ documentation for details on available arguments.

# Persistence ID (if used for internal tracking)
persistenceIdWidget.label=Persistence ID
persistenceIdWidget.description=Internal identifier for tracking persistence of messages or queues.
